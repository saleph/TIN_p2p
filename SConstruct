# Compilation:
# $ scons: everything
# $ scons tests: only tests
# $ scons app: app only

# MAGIC SEQUENCE: win32 encoding problems
import sys
reload(sys)
sys.setdefaultencoding('utf8')
# END OF MAGIC SEQUENCE

SConscript('SConscript.scons', variant_dir='BUILD', duplicate=0)

